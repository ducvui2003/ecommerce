import { ReactNode } from 'react';
type OrderManagerDetailProps = {
  params: Promise<{ id: string }>;
};

const OrderManagerDetail = ({ params }: OrderManagerDetailProps) => {
  return <div></div>;
};

export default OrderManagerDetail;
