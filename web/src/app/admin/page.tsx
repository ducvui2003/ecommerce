import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Dashboard',
  description: 'Oil',
};

const AdminPage = () => {
  return (
    <>
      <div>Dashboard</div>
    </>
  );
};

export default AdminPage;
